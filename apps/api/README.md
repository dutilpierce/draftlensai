# DraftLens API

Install with `pip install -e ".[dev]"` from this directory, then:

```powershell
uvicorn draftlens_api.main:app --reload --host 127.0.0.1 --port 8000
```

Copy `.env` from the repo root `.env.example` (see main README).
